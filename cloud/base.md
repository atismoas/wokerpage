**IP分类**

A类： 1-127

B类： 128-191

C类： 192-223

CSS will-change 告诉浏览器可能会发生变化的属性，由浏览器决定是否分层

transfrom效率高的原因是因为它不在渲染主线程里面，它发生在合成线程里

text-align 会对它的子元素中的行盒元素居中，例如audio， 但是div不会div不是行盒，文字是行盒

SSAA FSAA
超采样抗锯齿技术
将用两倍的尺寸渲染，然后缩放回原尺寸，每一个像素由四个像素平均而来

MSAA 多采样抗锯齿
边缘像素由临近像素计算而来

*** Perlin Noise ***
https://gist.github.com/patriciogonzalezvivo/670c22f3966e662d2f83
